if(ENABLE_TESTS)
  enable_testing()
endif()
file(MAKE_DIRECTORY ${CMAKE_BINARY_DIR}/tools)
file(GLOB TOOL_DIRS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} */)
foreach(TOOLD ${TOOL_DIRS})
  if(IS_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/${TOOLD})
    add_subdirectory(${TOOLD})
  endif()
endforeach()
